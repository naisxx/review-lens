# Dependencies
node_modules

# Build output
dist
.output
.nitro
.tanstack

# Netlify (local)
.netlify

# Vite / local files
*.local
node_modules/.vite

# Generated route tree (regenerated by `npm run dev` / `npm run build`)
src/routeTree.gen.ts

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db

# Local AI / tooling config (not shared)
CLAUDE.local.md
.claude

# Raw review corpora (source data — aggregated into src/data/cube.json, never committed)
*.xlsx
*.xls
